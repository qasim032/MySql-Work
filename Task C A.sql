select ename from emp where (job!="CLERK")And Deptno = 20 ;
select  job,deptno from emp order by deptno asc,job asc;
select sum(sal)AS "Sum" from emp ;
select  job from emp where job ="Clerk"order by job;
select  job from emp where ename = "James" order by job;
select * from dept ;
select  * from emp where ename = "James" order by job;
select  * from emp where ename = "James" order by job;
select * from emp;
